enum AuthStatus{SIGNIN , SIGNOUT, PROCESSING}
enum SignInMethodFlag{
  google, kakao, naver, apple
}
enum UserType{ PREMEMBER , MEMBER, MASTER, }
enum VerificationState {READY, RUNNING, FINISH}
enum TapOption {GOOD, LATEST}
enum SearchMode{INPUT, SEARCHING}